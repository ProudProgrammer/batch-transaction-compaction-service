management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      show-values: always

spring:
  cache:
    cache-names: cache1
    caffeine:
     spec: maximumSize=1000,expireAfterWrite=10m
